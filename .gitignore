__pycache__/
*.pyc
*.pyo

# Sampler frames — the pickled RunFrame history is 10-15 MB per run and is
# only needed to replay a run interactively. The .npz/.csv archives carry
# everything the figures and tables use.
*_frames.pkl

# Generated by the figure/table scripts. Everything here is reproduced in
# seconds from the archives under the *_output/ directories, so it is not
# tracked. See README.md for which script writes what.
figure_benchmark_2d.png
figure_benchmark_2d.pdf
tables_benchmark_2d.xlsx
tables_gtcd.xlsx
tables_tp_case_study.xlsx
trellis_gtcd_*.png
reference_*.png
reference_*.pdf
feasible_space_comparison*.png
tp_proposed_case_study.png
tp_design_space_serial*.png
tp_live_serial.png

# HYSYS working copies
methanol_instance*.hsc
*.bkp
